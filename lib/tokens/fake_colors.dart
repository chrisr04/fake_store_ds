import 'dart:ui';

/// A class that defines a set of custom colors used throughout the application.
class FakeColors {
  FakeColors._();

  // Purple color palette
  /// A pure purple color.
  static const purple = Color(0xFF7933FF);

  /// A 10% opacity purple color.
  static const purple10 = Color(0x177933FF);

  /// A 20% opacity purple color.
  static const purple20 = Color(0x337933FF);

  /// A dark shade of purple.
  static const purpleDark = Color(0xFF5646A5);

  /// A light shade of purple.
  static const purpleLight = Color(0xFFA781FF);

  // Grey color palette
  /// A standard grey color.
  static const grey = Color(0xFFE8E8EE);

  /// A 6% opacity grey color.
  static const grey6 = Color(0x0F303969);

  /// A 10% opacity grey color.
  static const grey10 = Color(0x18303969);

  /// A 15% opacity grey color.
  static const grey15 = Color(0x25303969);

  /// A 20% opacity grey color.
  static const grey20 = Color(0x33303969);

  /// A 30% opacity grey color.
  static const grey30 = Color(0x4A303969);

  /// A 60% opacity grey color.
  static const grey60 = Color(0x99303969);

  /// A dark shade of grey.
  static const greyDark = Color(0xFF848492);

  /// A dark shade of grey, typically used for text.
  static const greyDarkText = Color(0xFF595D78);

  /// A light shade of grey.
  static const greyLight = Color(0xFFEFEFF5);

  /// A medium shade of grey.
  static const greyMedium = Color(0xFFE1E1E8);

  // Navy color palette
  /// A deep navy color.
  static const navy = Color(0xFF05192D);

  /// A 70% opacity navy color.
  static const navy70 = Color(0xB105192D);

  /// A dark shade of navy.
  static const navyDark = Color(0xFF000820);

  /// A light shade of navy.
  static const navyLight = Color(0xFF213147);

  /// A medium shade of navy.
  static const navyMedium = Color(0xFF13253A);

  // Orange color palette
  /// A bright orange color.
  static const orange = Color(0xFFFF931E);

  /// A 20% opacity orange color.
  static const orange20 = Color(0x33FFBC4B);

  /// A dark shade of orange.
  static const orangesDark = Color(0xFFD87300);

  /// A light shade of orange.
  static const orangeLight = Color(0xFFFFBC4B);

  // Red color palette
  /// A vibrant red color.
  static const red = Color(0xFFFF5400);

  /// A dark shade of red.
  static const redDark = Color(0xFFDD3400);

  /// A light shade of red.
  static const redLight = Color(0xFFFF782D);

  // Yellow color palette
  /// A bright yellow color.
  static const yellow = Color(0xFFFCCE0D);

  /// A dark shade of yellow.
  static const yellowDark = Color(0xFFCFA600);

  /// A light shade of yellow.
  static const yellowLight = Color(0xFFFFEC3C);

  // Green color palette
  /// A vibrant green color.
  static const green = Color(0xFF03EF62);

  /// A dark shade of green.
  static const greenDark = Color(0xFF00C74E);

  /// A light shade of green.
  static const greenLight = Color(0xFF65FF8F);

  // Basic colors
  /// A pure white color.
  static const white = Color(0xFFFFFFFF);

  /// A 10% opacity black color.
  static const black10 = Color(0x18000000);

  /// A 30% opacity black color.
  static const black30 = Color(0x4B000000);

  /// A 8% opacity black color.
  static const black8 = Color(0x13000000);
}
